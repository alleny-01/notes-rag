VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
# Set these only as Supabase Edge Function secrets, never as VITE_* variables.
# Local functions may load them from supabase/functions/.env (which stays ignored).
# DEEPSEEK_API_KEY=
# VOYAGE_API_KEY=
# DAILY_MESSAGE_LIMIT=50